/**
 * Utility functions exports
 */

export { storage } from './storage';
export { mockProducts, mockUsers, mockOrders, generateId } from './mockData';
export { formatCurrency, formatDate, formatDateTime, formatCEP, formatPhone } from './formatters';
